{
  "id": "capmation-workshop-env",
  "name": "Capmation Workshop API — Local",
  "values": [
    {
      "key": "baseUrl",
      "value": "http://localhost:3000",
      "type": "default",
      "enabled": true,
      "description": "URL base del servidor. Cambiar si el puerto es diferente."
    },
    {
      "key": "accessToken",
      "value": "",
      "type": "secret",
      "enabled": true,
      "description": "JWT access token (expira 15 min). Se guarda automáticamente al hacer Login o Refresh."
    },
    {
      "key": "refreshToken",
      "value": "",
      "type": "secret",
      "enabled": true,
      "description": "Refresh token opaco (expira 7 días). Se guarda automáticamente al hacer Login o Refresh. Se rota en cada uso."
    },
    {
      "key": "userId",
      "value": "",
      "type": "default",
      "enabled": true,
      "description": "ID del usuario autenticado. Se guarda automáticamente al hacer Login."
    },
    {
      "key": "targetUserId",
      "value": "",
      "type": "default",
      "enabled": true,
      "description": "ID del usuario a actualizar con PATCH /users/:id. Se guarda automáticamente al crear un usuario."
    }
  ],
  "_postman_variable_scope": "environment",
  "schema": "https://schema.getpostman.com/json/environment/v2.1.0/environment.json"
}
